# 로컬 검증 기록 — 2026-08-31

의료데이터 없이 코드가 생성한 합성 영상만 사용했습니다. 아래 결과는 구현 검증이며 모델의 의료적 성능·안심존 GPU 호환성 검증이 아닙니다.

## 실행 환경

- macOS ARM64, CPython 3.12.13, CPU
- torch 2.12.1, torchvision 0.27.1
- NumPy 2.2.6, Pillow 12.1.0, matplotlib 3.10.8
- 임시 가상환경에 설치하여 기존 Python 환경은 변경하지 않음
- `pip check`: 의존성 충돌 없음
- 전체 Python 파일의 Python 3.10 구문 검사 및 pyproject TOML 파싱 통과

Ubuntu 20.04 x86_64 / CPython 3.10 / torch 2.12.1+cu126 / torchvision 0.27.1+cu126의 실행 검증을 의미하지 않습니다. CUDA 커널·혼합 정밀도·GPU 메모리와 실제 승인된 ImageNet 파일은 현장 확인이 필요합니다.

## 결과

```bash
IMPLANT_RUN_ML_TESTS=1 python -m unittest discover -s implant_analysis/tests -v
```

**18개 테스트 통과.** 데이터 감사 12개, 분할 검증·지표 5개, 실제 ResNet50 학습 통합 테스트 1개입니다. ML 시험은 기본 테스트 실행에서는 건너뛰며 위 환경변수로 명시적으로 실행합니다.

통합 시험 하나 안에서 다음 경로를 실행했습니다.

1. 합성 분할 초안을 검토 기록과 함께 확정.
2. 가중치 누락과 SHA-256 불일치 시 학습 거절.
3. 로컬에서 생성한 1,000클래스 ResNet50 state_dict를 읽고 출력층을 교체하여 실제 CPU 학습. 공식 ImageNet 가중치를 사용한 성능 검증은 아님.
4. 무작위 초기화 합성 시험으로 1 epoch 학습 후 2 epoch까지 재개.
5. 동일 seed의 연속 2 epoch 실행과 **학습 기록 및 최종 모델의 모든 tensor가 일치**함을 확인.
6. calibration/test 영상 파일이 접근 불가능한 상태에서도 학습과 dev 평가가 완료됨을 확인.
7. 최종 평가 기록이 없으면 test 평가 거절, 기록이 있으면 합성 test 평가 실행.
8. PNG/PDF 혼동행렬·학습 곡선 생성. 다른 모델의 학습 곡선을 연결하면 거절.
9. 감사 이후 영상 변경, 확정 이후 manifest 변경 시 거절.

테스트 중 torchvision 다운로드 경로도 실패하도록 막았습니다. 소스의 모델 생성은 `weights=None`만 사용하고 로컬 파일은 `weights_only=True`로 읽습니다. 이 시험은 운영체제 수준의 네트워크 격리 검증은 아닙니다.

혼동행렬 그림을 별도의 합성 집계값으로 생성하고 축·클래스 번호·색상 눈금·셀 표기를 육안 확인했습니다. 합성 예시 수치를 연구 결과로 보고하거나 저장소에 이미지로 포함하지 않습니다.

## 아직 검증하지 않은 것

- 실제 JSON 구조와 영상 대응 정확도, 환자·원촬영 그룹키의 의미
- 실제 제품 불균형, 희귀 제품의 검증 가능성, 임상적 오분류 위험
- 대규모 데이터에서의 학습 시간·메모리·재개 I/O 비용
- 실제 ImageNet 가중치 반입 파일과 GPU에서의 실행
- 미학습 제품 거절, 확률 보정, 그룹 신뢰구간, Grad-CAM

현재 체크포인트는 마지막 모델과 최적 모델, optimizer를 함께 저장하므로 수백 MB가 될 수 있습니다. 원본 데이터와 분리된 승인된 작업 저장소의 여유 공간을 확인해야 합니다.


## 2026-09-14 현장 점검 코드 검증

Python 3.12.14 / macOS ARM64 / Pillow 12.3.0에서 데이터 점검 테스트 24개가 통과했습니다. 전체 검색된 25개 중 ML 통합 시험 1개는 선택 실행 항목으로 이번 변경에서는 건너뛰었습니다. GPU·학습 코드는 이번에 변경하지 않았습니다. Python 3.10 구문 검사, 예시 JSON/TOML 파싱, visit CLI 실행과 제공 분할 설정의 train 직접 실행 차단도 확인했습니다.

추가한 7개 합성 테스트는 제공 영역 보존 및 동일 파일명 충돌 방지, 영역 간 같은 픽셀·서로 다른 라벨 탐지, 상위 폴더 분산 표본과 부분 점검 표시, preflight의 파일 본문 미열람, 한 영역 실패 시 부분 실패 표시, 경로 중복·이탈 방지, 기존 실행 덮어쓰기 방지, 표본 재현성을 포함합니다. 점검 전후 합성 원본 파일의 해시가 같은지도 확인했습니다.

최대 30개 크롭을 보여주는 내부 미리보기의 이미지 비율과 번호·영역 표시를 합성 이미지로 확인했습니다. HTML 요약 화면은 외부 URL/JavaScript를 사용하지 않으며 텍스트 요약을 함께 제공합니다. 실제 안심존 웹 브라우저 표시 여부는 미확인입니다.

실제 의료데이터는 읽거나 반출하지 않았습니다. 기존의 18개 ML 포함 검증 기록은 8월 31일 기록이며 이번에 재실행했다고 해석하지 않습니다. visit은 제공 Training/Validation의 점검 도구이며, 제공 분할을 사용하는 학습 경로는 원분할 정책 확인 후 추가로 연결해야 합니다.
