# 방문 후 전달 양식 — 빈 양식만 Git에 보관

**기관이 외부 설명을 허용한 항목만 작성합니다.** 아래 텍스트도 자동 반출 승인 자료가 아닙니다. 작성한 결과를 Git에 넣지 않습니다. 원본 사례의 파일명·일련번호·날짜·경로·기관 식별정보는 이 양식에 넣지 않습니다. 공유 불가/미확인은 유효한 답변입니다.

## A. 다음 개발 결정에 필요한 최소 답변

```text
[방문 결과 설명 — 허용된 항목만]
1. 실행한 단계: preflight / sample / full
2. 각 단계 완료 상태: 완료 / 일부 실패 / 중단 / 미실행
3. Python·주요 패키지: 승인 버전과 일치 / 불일치 / 미확인 (허용된 버전 정보: )
4. GPU: CUDA 작은 연산 성공 / 사용 불가 / 오류 / 미확인
5. 승인 가중치: 해시 일치 / 파일 없음 / 해시 없음 / 불일치 / 미확인
6. 제공 영역: Training·Validation 모두 확인 / 일부 누락 / 압축 상태 / 경로 수정 필요
7. JSON–JPEG 매칭: 상대경로+파일명 본체 일치 / 별도 규칙 필요 / 미확인
8. JSON 구조: 예시의 한국어 필드 10개 형태 / 중첩·키 차이 / 미확인
9. sample 수동 확인: 매칭 문제 없음 / 문제 있음 / 미실시; 크롭에서 확인한 일반적 문제 유형:
10. full 오류: 없음 / 있음 / 미완료 (허용된 오류 종류만: )
11. 두 영역의 정확한 픽셀 중복: 관측됨 / 전체 점검에서 0 / 표본에서만 0 / 미확인
12. Validation에만 있는 제품: 관측됨 / 전체 점검에서 없음 / 표본 결과뿐 / 미확인
13. 일련번호의 기관 설명: 환자 단위 / 원촬영 단위 / 임플란트·레코드 단위 / 미확인
14. 별도 그룹키: 환자키 있음 / 원촬영키만 있음 / 없음 / 제공 확인 중
15. 원래 Training/Validation 분할 기준·재분할 허용 범위에 대한 기관 설명:
16. 가장 먼저 해결해야 할 문제 한 가지:
17. 다음 방문까지 코드 수정본 반입 가능 여부·예상 소요기간:
```

## B. 집계 수치가 허용될 때만 추가

```text
[내부 READ_ON_SCREEN.txt에서 확인한 허용 집계]
Training: JSON 수 / 영상 수 / 점검 수 / 제외 수 / 사용가능 제품 수:
Validation: JSON 수 / 영상 수 / 점검 수 / 제외 수 / 사용가능 제품 수:
매칭 실패·JSON 오류·손상영상·필수 라벨 누락 수:
출처 폴더별 누락 여부; h코드와 JSON 병원 대응의 일관성:
두 영역의 정확한 중복 픽셀 군집 수 / 그중 라벨 충돌 군집 수:
Training에만 관측된 제품 수 / Validation에만 관측된 제품 수:
영상 폭·높이의 최소/중앙/최대(점검 범위도 함께):
전체 감사 소요시간 / 작업 저장공간 여유:
```

## C. 학습 이후 방문용

```text
[학습·평가 — 허용된 항목만]
실험명·코드 버전(기관 내부 경로 제외):
분할 정책과 그룹 단위 확인 여부:
학습 상태 / 완료 epoch / 최적 dev epoch:
batch size / epoch 소요시간 / GPU 메모리 범위:
train loss·dev 지표의 일반적인 흐름:
허용된 dev macro-F1 / accuracy / Top-3:
보정·미학습 제품 실험·최종 test 평가: 실행 / 미실행
최종 test를 이미 평가했는지 여부:
부가 분석(제품·병원·촬영방법) 완료 여부:
다음 실행에 영향을 주는 문제:
```

오류를 설명할 때는 `어느 단계에서 + 어떤 오류 종류가 + 언제 발생했는지`를 우선 적습니다. 경로·사례 식별자가 포함된 traceback이나 화면 전체를 그대로 옮기지 않습니다. 예: “sample 시작 직후 KeyError, JSON이 객체가 아니라 배열로 들어 있음”처럼 허용된 구조 설명이면 됩니다. 수치를 허용받지 못한 경우 수치를 생략해도 다음 코드 수정에 사용할 수 있습니다.
