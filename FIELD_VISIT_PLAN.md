# 본선 현장 실행 계획 — 2026-09-14

첫 방문의 완료 기준은 **모델 성능 수치**가 아니라, 다음 방문의 학습을 어떤 데이터·분할·설정으로 실행할지 결정할 수 있는 점검 결과입니다. 결과 파일을 외부로 옮기지 않아도, 허용된 상태·집계와 기관 설명을 방문 후 전달하면 다음 개발을 결정할 수 있도록 구성했습니다.

## 새로 확인한 데이터 구조와 기존안 수정

설정의 data_root는 실제 마운트 경로 아래 **01.데이터 폴더**로 지정합니다. 아래 상대경로를 각각 보존합니다.

```text
01.데이터/
  1.Training/
    라벨링데이터/h01 … h15/h01_01 …/*.json
    원천데이터/h01 … h15/h01_01 …/*.jpeg
  2.Validation/
    라벨링데이터/h01 … h15/h01_01 …/*.json
    원천데이터/h01 … h15/h01_01 …/*.jpeg
```

실제 폴더명은 `원천데이터`입니다. 이전 예시의 `원시데이터/병원별데이터`와 다릅니다. h01~h15는 우선 출처 폴더 코드로만 기록하며, 실제 병원 대응은 JSON의 촬영병원과 기관 설명으로 확인합니다. h01_01 등의 하위 폴더는 재귀적으로 읽습니다. JPEG 확장자는 대소문자를 구분하지 않습니다.

JSON–영상은 **같은 제공 영역 안의 동일 상대경로·파일명 본체**로 매칭합니다. 예를 들어 라벨 h01/h01_01/a.json은 해당 영역 원천데이터의 h01/h01_01/a.jpeg와 대응합니다. 파일명 규칙이 다르면 자동 추측하지 않고 mapping CSV 설정을 사용합니다. 두 영역에 같은 파일명이 있어도 서로 다른 사례로 관리합니다.

앞서 예시로 설정한 전체 자료의 60/15/10/15 재분할은 보류합니다. 제공 Training/Validation을 합치지 않고 원래 기준과 이용 조건부터 확인합니다. 현재 visit 명령은 점검만 수행하고 학습용 분할을 생성하지 않습니다. 기존 학습 코드는 한 데이터 루트의 확정된 4분할을 가정하므로, 제공 분할을 사용하는 학습 연결은 현장 확인 후 수정해야 합니다.

## JSON 필드의 역할

첨부 설명자료의 어노테이션 정의(p.6~7)와 JSON 예시(p.8)를 기준으로 합니다. 실제 파일과의 일치 여부는 sample 단계에서 확인합니다.

| 필드 | 현재 사용 | 첫 방문 확인 |
|---|---|---|
| 제조사, 모델명 | 영상이 예측할 정답: 두 값을 합친 제품 라벨 | 빈 값, 표기 변형, 괄호 코드, 별칭 |
| 촬영방법 | 파노라마/치근단 등 하위집단 분석 | 실제 값 종류, 혼합 여부 |
| 촬영병원 | 병원별 분포·성능 분석 | h코드와 대응 관계 |
| 일련번호 | 원문 보존, 추적용 메타데이터 | 환자·원촬영·개별 임플란트 중 무엇의 번호인지 |
| 직경, 길이 | 분포·오류 사례 해석용 | mm 단위, 누락·형식 |
| 식립위치 | 메타데이터 | 값 형식과 누락 |
| 식립일자, 촬영일자 | 내부 점검용 메타데이터 | 날짜 형식·누락 |

입력은 임플란트 크롭 영상만 사용합니다. 직경·길이·날짜·병원 정보를 분류기 입력에 넣지 않습니다. 일련번호는 PDF에서 “병원별로 부여된 일련번호”라고 설명하며, 환자 ID라는 근거는 없습니다. PDF에 소개된 나사산·연결부 등 형태 설명을 실제 JSON에 존재하는 추가 정답 라벨로 간주하지 않습니다. 설명서의 전체 구축 규모·제품 수를 실제 제공 파일 수와 동일하다고 가정하지 않습니다.

## 반입할 구성

| 구성 | 목적 | 필수 여부 |
|---|---|---|
| implant_analysis 전체 소스와 tests | 점검, 기존 학습·평가 코드, 합성 시험 | 필수 |
| configs/visit.example.json | 새 구조의 첫 방문 설정 | 필수 |
| FIELD_VISIT_PLAN.md, FIELD_FEEDBACK_TEMPLATE.md, FIRST_VISIT_QUICKSTART.md | 실행·기록·후속 의사결정 | 필수 |
| README, TRAINING, VALIDATION 및 requirements 문서 | 기존 사용법과 검증 범위 | 필수 |
| 승인된 패키지와 ResNet50 가중치 | 현장 실행 | 기관 사전 설치·반입 절차로 별도 확보 |

visit 점검에는 Python 표준 라이브러리와 Pillow를 사용합니다. `--check-ml`을 켜면 설치된 torch/torchvision을 읽고 CUDA의 작은 연산까지 점검합니다. 가중치 경로와 전체 SHA-256을 설정하면 파일 동일성도 검사합니다. 이 점검은 ResNet50 본학습의 GPU 메모리·속도 보장은 아닙니다. 필요한 모델·패키지·소스·문서·코드 수정본을 어떤 방식으로 반입할 수 있는지와 추가 반입 소요기간도 함께 확인해야 합니다. 개인 전자기기·저장매체를 임의로 반입하는 흐름은 전제하지 않습니다.

## 방문 1: 구조·환경·검증 기준 확보

한 사람이 실행을 맡고 다른 사람이 화면 점검·기관 문의를 기록하면 좋습니다. 한 명이면 아래 순서대로 진행합니다. 시간은 예약과 실제 읽기 속도에 맞춰 조정합니다.

| 순서 | 실행 | 화면에서 확인 | 안심존 내부 산출물 |
|---|---|---|---|
| 1 | preflight | Python/패키지, CUDA, 파일 수, 각 h폴더 존재, 저장공간 | 환경·폴더 요약 |
| 2 | sample | 기본 각 영역 150개, 폴더별 분산 표본의 매칭·스키마·손상 | 샘플 감사, 미리보기, 대조표 |
| 3 | 수동 검토 | 매칭 예시 20~30개, 크롭의 끝부분/방향/문자/이상 여백 | 내부 검토 메모 |
| 4 | 담당자 확인 | 제공 분할 기준, 환자/원촬영키, h코드 의미 | 기관 답변·확인 근거 |
| 5 | full | 전체 읽기 성공, 제품·병원·촬영방법 분포, 영역 간 정확 중복 | 전체 감사·중복 표 |
| 6 | 귀가 전 | 단계별 완료 여부와 다음 장애물 확인 | 방문 기록, 허용된 전달 항목 |

preflight는 파일 목록을 읽되 JSON/영상 본문은 열지 않습니다. sample은 각 영역의 상위 폴더에서 번갈아 표본을 뽑습니다. 기본 150개씩이므로 총 300개 이내이며, 실제 파일 수에 따라 줄어듭니다. 이는 폴더별 구조 점검 표본이며 제품별 대표성을 보장하는 표본은 아닙니다. 수동 검토용 미리보기는 최대 30개만 표시합니다. 더 확인할 때는 internal 감사 manifest와 원본을 내부에서 대조합니다.

full은 모든 JSON과 대응 영상을 읽고 이미지 파일 해시·디코딩 픽셀 해시를 계산합니다. 걸리는 시간은 파일 수·저장장치에 따라 달라집니다. 중단된 실행을 이어받는 기능은 현재 visit 감사에 없으므로, 시간 부족 시 새로운 run-id로 다시 실행합니다. 완료 전 summary에 RUNNING이 남으면 완료 결과로 보고하지 않습니다. 원본 파일은 수정하지 않습니다.

## 첫 방문의 필수 확인 질문

1. **일련번호는 어떤 단위를 식별하는가?** 같은 환자의 여러 임플란트·여러 촬영을 묶을 수 있는 별도 키가 있는가? 이미 크롭된 여러 영상의 원촬영 키가 있는가? 기관 간 번호 범위는 어떤가?
2. **Training/Validation은 어떤 단위로 나눈 것인가?** 환자/원촬영 독립성이 보장되는가? 원래 validation의 용도, 재분할 허용 범위, 별도 Test 제공 여부는 무엇인가?
3. h01~h15와 JSON 촬영병원은 일대일로 대응하는가? JSON–JPEG의 공식 대응 규칙이 있는가?
4. 작업 결과·체크포인트가 방문 사이에 보존되는가? 운영시간 후 작업이 계속 실행되는가? 코드 수정본의 추가 반입은 언제 가능한가?
5. 코드 실행 상태, 오류 유형, 집계값, 성능 지표 중 어느 항목을 구두·메모로 외부 설명할 수 있는가? 방문 증빙은 어떤 형식으로 남기는가?

환자키가 없다고 코드 검증을 멈출 필요는 없습니다. 환경·파일·크롭·분포·중복 감사는 진행할 수 있습니다. 다만 환자 독립성을 입증하지 못한 상태에서 임의 번호로 키를 만들거나, 이미지 무작위 분할 결과를 환자 독립 성능으로 보고하지 않습니다.

## 현장 결과에 따른 분기

| 관찰 결과 | 후속 작업 |
|---|---|
| JSON–JPEG 파일명 본체가 일치 | 상대경로 매칭 유지, 수동 검토 근거 기록 |
| 파일명 규칙이 다름 | 기관 규칙 또는 내부 대응표로 mapping CSV 설정; 순서로 매칭하지 않음 |
| JSON 필드·중첩 구조가 다름 | fields / record_path 설정 수정; 키 구조 설명만으로 조정 가능 |
| 라벨 별칭이 있음 | 내부 별칭 표 작성, 괄호·대소문자를 무조건 지우지 않음 |
| 두 제공 영역에 같은 픽셀이 있음 | 내부 중복표 검토, 제외 원칙을 먼저 정하고 기록; 자동 이동·삭제 없음 |
| Validation에만 제품이 있음 | 라벨 별칭 문제인지 실제 미학습 제품인지 확인, closed-set 범위와 unknown 실험을 구분 |
| 환자/원촬영키 확보 | 제공 영역 사이의 그룹 중복을 별도로 검사하고 학습 분할 정책 확정 |
| 키 없음·원분할 기준 미확인 | 자료 품질 보고와 실행 시험까지 진행, 검증 가능한 성능 주장의 범위를 재설계 |
| CPU만 사용 가능 | 처리량을 확인해 동결 특징/작은 모델 등 계산량 축소안을 선택 |
| 패키지·가중치 불일치 | 완료된 데이터 감사 결과를 보존하고 설치·반입 문제부터 해결 |

## 방문 2 이후의 본선 실험 구성

제공 분할이 환자 또는 원촬영 단위로 독립적이고 그 용도가 허용되면, **제공 Validation을 최종 평가용으로 보존하고 제공 Training 안에서 train/dev/calibration을 그룹 단위로 나누는 안**을 우선 검토합니다. 내부 비율은 70/15/15를 검토 출발점으로 삼되 그룹 수·희귀 제품 수를 확인한 뒤 확정합니다. 제공 Validation이 독립 시험 집합이라는 사실은 폴더명만으로 증명되지 않습니다. 이 설계는 아직 현행 학습 코드에 연결되지 않았습니다.

| 방문/단계 | 목표 | 확인해서 설명할 내용 | 내부에 남길 자료 |
|---|---|---|---|
| 2 | 확인한 분할 정책 연결, 전체 감사 재확인, 소량 GPU 학습 시험 | 분할 누수 검사 결과, 제품 수, 배치 크기·epoch 시간·메모리 | 확정 manifest, 환경 기록, 처리량 |
| 3 | ResNet50 기준모델 학습 완주 | 완료/중단, loss 흐름, 최적 dev epoch·macro-F1, 소요시간 | last/best 상태, 로그·예측 |
| 4 | dev 기준 오류·하위집단 분석, 필요 시 사전에 정한 단일 비교실험 | 개선 여부, 주로 혼동되는 제품군의 허용된 집계, 하위집단 표본 충분 여부 | 비교표, dev 그림 |
| 5 | 확률 보정·거절 또는 그룹 신뢰구간 등 우선 확장 | calibration 처리 완료와 고정된 기준 | 보정 설정·평가 코드·기록 |
| 6 | 선택 확정 후 최종 평가 | 전체·제품별 지표, 신뢰구간, 부가 분석 완료 여부 | test 평가·최종 그림 |
| 7 | 보고서·발표 검수와 허용된 산출물 반출 신청 | 제출물 완성, 증빙 누락, 허용 범위 | 결과보고서·발표 PDF·방문 증빙 |

이는 7회 방문을 기준으로 한 실행 배치안입니다. 같은 날짜의 팀원 여러 명 방문은 별도 방문으로 세지 않으며, 안내된 평가기준에 맞춰 날짜·시간 증빙을 남깁니다. 짧은 기간에는 방문을 확보하되 실제 분석 필요에 맞춰 단계 일부를 합칩니다. 설치·추가 반입이 2~3주 걸릴 수 있으므로 확장 코드를 매 방문마다 새로 반입할 수 있다는 가정으로 일정을 잡지 않습니다.

필수 산출물은 자료 품질·사용 규모 표, 분할 근거와 누수 점검, 기준모델 결과, 병원/촬영방법별 분석, 오류 사례 해석, 재현 가능한 코드·환경 기록입니다. 확률 보정과 unknown 거절은 같은 기능이 아닙니다. 미학습 제품 실험에는 학습 전 제외 제품 확정과 별도 학습 설계가 필요하며, calibration만 있다고 unknown 검증이 완성되지 않습니다. 현행 코드에는 확률 보정·거절·그룹 bootstrap·Grad-CAM이 아직 없습니다.

## 화면 확인과 외부 전달의 구분

`dashboard.html`과 `READ_ON_SCREEN.txt`는 인터넷 없이 내부 화면에서 읽는 도구입니다. 상세 manifest·오류표·중복표·미리보기·모델 출력은 모두 안심존에 남깁니다. 텍스트나 구두 설명도 그 자체로 반출 제한에서 면제되는 것은 아니므로, 허용받은 항목만 FIELD_FEEDBACK_TEMPLATE에 따라 전달합니다. 허용되지 않은 항목에는 “공유 불가”, 확인하지 못한 항목에는 “미확인”이라고 기록하면 됩니다.

정확한 수치 공유가 어려워도 **단계 완료 여부, 코드 오류 유형, 매칭 규칙 성립 여부, 그룹키 의미와 원분할 정책**을 허용 범위 내에서 설명하면 다음 코드 변경을 결정할 수 있습니다. 오류 traceback을 그대로 전달하기보다 stage·오류 종류와 경로를 제거한 메시지로 설명합니다. 화면을 암기해 옮기는 방식으로 반출 규정을 우회하지 않습니다.
